import { shallowMount } from '@vue/test-utils';
import FormContainer from '@/components/form-container/FormContainer.vue';

describe('FormContainer.vue', () => {
  it('placeholder', () => {
    expect(true).toEqual(true);
  });
});
